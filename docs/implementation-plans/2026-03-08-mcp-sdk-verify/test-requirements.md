# Test Requirements: MCP SDK Verification

## Automated Verification

| AC ID                | Criterion                                                                                                                          | Verification Command                                                                                                                                                                                                                                                                                                             | Expected Result                                                                                                                                        |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- | --- | ----- | --- | -------------------------------------------- | ------------------------------------------------------------------------------- |
| mcp-sdk-verify.AC1.1 | `@modelcontextprotocol/sdk` appears in `dependencies` (not `devDependencies`) in `package.json` at version `^1.27`                 | `node --input-type=module -e "import { readFileSync } from 'node:fs'; const pkg = JSON.parse(readFileSync('package.json', 'utf8')); const inDeps = '@modelcontextprotocol/sdk' in pkg.dependencies; const ver = pkg.dependencies['@modelcontextprotocol/sdk']; const inDev = '@modelcontextprotocol/sdk' in (pkg.devDependencies |                                                                                                                                                        | {}); if (!inDeps |     | inDev |     | !ver.startsWith('^1.27')) process.exit(1);"` | Exit code 0. Package is in `dependencies` at `^1.27`, not in `devDependencies`. |
| mcp-sdk-verify.AC1.2 | `pnpm install` succeeds and the package is resolvable                                                                              | `pnpm install && pnpm ls @modelcontextprotocol/sdk`                                                                                                                                                                                                                                                                              | Exit code 0. Package appears in dependency tree output.                                                                                                |
| mcp-sdk-verify.AC1.3 | `pnpm typecheck` passes with the SDK installed                                                                                     | `pnpm typecheck`                                                                                                                                                                                                                                                                                                                 | Exit code 0. No type errors.                                                                                                                           |
| mcp-sdk-verify.AC2.1 | `scripts/verify-sdk.ts` exists and is executable via `npx tsx scripts/verify-sdk.ts`                                               | `test -f scripts/verify-sdk.ts && npx tsx scripts/verify-sdk.ts`                                                                                                                                                                                                                                                                 | Exit code 0. File exists and script runs without error.                                                                                                |
| mcp-sdk-verify.AC2.2 | Script imports `McpServer`, `StdioServerTransport`, `ResourceTemplate` from their correct SDK paths                                | `rg "import.*McpServer.*from" scripts/verify-sdk.ts && rg "import.*StdioServerTransport.*from" scripts/verify-sdk.ts && rg "import.*ResourceTemplate.*from" scripts/verify-sdk.ts`                                                                                                                                               | Exit code 0. All three import statements are present in the script.                                                                                    |
| mcp-sdk-verify.AC2.3 | Script imports the `CallToolResult` type and uses it in a type annotation                                                          | `rg "import type.*CallToolResult" scripts/verify-sdk.ts && rg ":\s*CallToolResult" scripts/verify-sdk.ts`                                                                                                                                                                                                                        | Exit code 0. Type import and type annotation both present.                                                                                             |
| mcp-sdk-verify.AC2.4 | Script verifies `registerTool`, `registerResource`, `sendResourceListChanged`, and `connect` methods exist on `McpServer` instance | `rg "registerTool" scripts/verify-sdk.ts && rg "registerResource" scripts/verify-sdk.ts && rg "sendResourceListChanged" scripts/verify-sdk.ts && rg "connect" scripts/verify-sdk.ts`                                                                                                                                             | Exit code 0. All four method names referenced in the script.                                                                                           |
| mcp-sdk-verify.AC2.5 | Script outputs verification results to stderr (not stdout)                                                                         | `npx tsx scripts/verify-sdk.ts 2>/dev/null 1>stdout_capture.tmp; test ! -s stdout_capture.tmp && rm stdout_capture.tmp`                                                                                                                                                                                                          | Exit code 0. stdout capture file is empty (all output went to stderr).                                                                                 |
| mcp-sdk-verify.AC2.6 | Script exits with code 0 when all verifications pass                                                                               | `npx tsx scripts/verify-sdk.ts 2>/dev/null; echo $?`                                                                                                                                                                                                                                                                             | Output is `0`.                                                                                                                                         |
| mcp-sdk-verify.AC3.1 | `docs/verified-api.md` exists and documents import paths for all verified constructs                                               | `test -f docs/verified-api.md && rg "Import Path" docs/verified-api.md && rg "McpServer" docs/verified-api.md && rg "StdioServerTransport" docs/verified-api.md && rg "ResourceTemplate" docs/verified-api.md && rg "CallToolResult" docs/verified-api.md`                                                                       | Exit code 0. File exists and contains import path documentation for all four constructs.                                                               |
| mcp-sdk-verify.AC3.2 | Document includes constructor signatures for `McpServer`, `StdioServerTransport`, and `ResourceTemplate`                           | `rg "new McpServer" docs/verified-api.md && rg "new StdioServerTransport" docs/verified-api.md && rg "new ResourceTemplate" docs/verified-api.md`                                                                                                                                                                                | Exit code 0. All three constructor examples are present.                                                                                               |
| mcp-sdk-verify.AC3.3 | Document includes method signatures for `registerTool`, `registerResource`, `sendResourceListChanged`, and `connect`               | `rg "registerTool" docs/verified-api.md && rg "registerResource" docs/verified-api.md && rg "sendResourceListChanged" docs/verified-api.md && rg "connect" docs/verified-api.md`                                                                                                                                                 | Exit code 0. All four method names documented.                                                                                                         |
| mcp-sdk-verify.AC3.4 | Document includes a "Discrepancies" section with a table of architecture doc assumptions vs actual SDK API                         | `rg "Discrepancies" docs/verified-api.md && rg "Architecture Doc" docs/verified-api.md && rg "Actual SDK" docs/verified-api.md`                                                                                                                                                                                                  | Exit code 0. Discrepancies section exists with comparison table headings.                                                                              |
| mcp-sdk-verify.AC4.1 | Import path discrepancies are flagged (arch doc paths vs actual SDK paths)                                                         | `rg "server/mcp.js" docs/verified-api.md && rg "server/stdio.js" docs/verified-api.md`                                                                                                                                                                                                                                           | Exit code 0. The architecture doc's assumed import subpaths appear in the discrepancies table for comparison.                                          |
| mcp-sdk-verify.AC4.2 | Notification method discrepancy is flagged (`notification()` vs `sendResourceListChanged()`)                                       | `rg "notification\(" docs/verified-api.md && rg "sendResourceListChanged" docs/verified-api.md`                                                                                                                                                                                                                                  | Exit code 0. Both the architecture doc's `notification()` pattern and the actual `sendResourceListChanged()` method appear in the discrepancies table. |
| mcp-sdk-verify.AC4.3 | Resource callback signature discrepancy is flagged (`(uri, {uid})` vs `(uri, variables, extra)`)                                   | `rg "uri.*uid" docs/verified-api.md && rg "uri.*variables.*extra" docs/verified-api.md`                                                                                                                                                                                                                                          | Exit code 0. Both the old assumed callback shape and the actual callback shape are documented.                                                         |

## Human Verification

| AC ID                | Criterion                                                   | Verification Approach                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| mcp-sdk-verify.AC4.4 | Each discrepancy entry includes the corrected usage pattern | Open `docs/verified-api.md` and locate the discrepancies table (Section 9). Confirm that all 4 discrepancy rows (import: McpServer, import: Transport, notifications, resource callback) each have a non-empty "Corrected Usage" column containing a code snippet or pattern that a downstream implementor can copy directly. Verify each corrected pattern is consistent with the import paths and signatures documented in Sections 2-7 of the same document. |
