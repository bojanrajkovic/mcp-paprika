# Test Requirements: Vector Store (p3-u04)

## Automated Tests

| AC                        | Criterion                                                      | Test Type   | Test File                                     | Test Description                                                                                                                                                 |
| ------------------------- | -------------------------------------------------------------- | ----------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| p3-u04-vector-store.AC1.1 | init creates index on first run                                | unit        | src/features/vector-store.test.ts             | No hash-index.json on disk, `isIndexCreated()` returns false — verify `createIndex()` called and `size` is 0                                                     |
| p3-u04-vector-store.AC1.1 | init creates index on first run                                | integration | src/features/vector-store.test.integration.ts | Init on empty temp dir — verify `vectors/` directory and `index.json` created on disk                                                                            |
| p3-u04-vector-store.AC1.2 | init loads existing state on subsequent runs                   | unit        | src/features/vector-store.test.ts             | Write valid hash-index.json before init — verify `size` matches entries and `createIndex()` NOT called                                                           |
| p3-u04-vector-store.AC1.3 | init recovers from corrupted hash-index.json (invalid JSON)    | unit        | src/features/vector-store.test.ts             | Write invalid JSON to hash-index.json — verify init succeeds, stderr contains warning, `.bak` file created, `size` is 0                                          |
| p3-u04-vector-store.AC1.3 | init recovers from corrupted hash-index.json (schema mismatch) | unit        | src/features/vector-store.test.ts             | Write `{"not": "valid"}` (wrong schema shape) to hash-index.json — verify same recovery behavior as invalid JSON                                                 |
| p3-u04-vector-store.AC1.4 | init recovers from corrupted Vectra index                      | unit        | src/features/vector-store.test.ts             | `isIndexCreated()` returns true but throws on subsequent Vectra operation — verify stderr warning, index recreated with `deleteIfExists: true`, hash map cleared |
| p3-u04-vector-store.AC2.1 | indexRecipes embeds and upserts changed recipes                | unit        | src/features/vector-store.test.ts             | Pass 2 recipes, `embedBatch` returns 2 vectors — verify `upsertItem` called twice with correct `id`, `vector`, and `metadata.recipeName`                         |
| p3-u04-vector-store.AC2.1 | indexRecipes embeds and upserts changed recipes                | integration | src/features/vector-store.test.integration.ts | Index 3 recipes with deterministic embedder, search — verify results contain recipe names and scores                                                             |
| p3-u04-vector-store.AC2.2 | indexRecipes skips unchanged recipes                           | unit        | src/features/vector-store.test.ts             | Index a recipe, then call `indexRecipes` again with same recipe — verify `embedBatch` NOT called, result has `skipped: 1`                                        |
| p3-u04-vector-store.AC2.2 | indexRecipes skips unchanged recipes                           | integration | src/features/vector-store.test.integration.ts | Index recipes, re-index same recipes — verify all skipped via `IndexingResult` counts                                                                            |
| p3-u04-vector-store.AC2.3 | indexRecipes returns correct IndexingResult counts             | unit        | src/features/vector-store.test.ts             | Index 3 recipes (2 new, 1 unchanged) — verify result is `{ indexed: 2, skipped: 1, total: 3 }`                                                                   |
| p3-u04-vector-store.AC2.4 | indexRecipes persists hash map after indexing                  | unit        | src/features/vector-store.test.ts             | After indexing, read `hash-index.json` from temp dir — verify file contains entries for indexed recipes                                                          |
| p3-u04-vector-store.AC2.5 | indexRecipes with empty array is a no-op                       | unit        | src/features/vector-store.test.ts             | `indexRecipes([])` returns `{ indexed: 0, skipped: 0, total: 0 }` and `embedBatch` NOT called                                                                    |
| p3-u04-vector-store.AC2.6 | hash map persists across restarts                              | unit        | src/features/vector-store.test.ts             | Index recipes, create new VectorStore instance on same temp dir, `init()`, index same recipes — verify all skipped (hashes loaded from disk)                     |
| p3-u04-vector-store.AC2.6 | hash map persists across restarts                              | integration | src/features/vector-store.test.integration.ts | Full end-to-end: index recipes, destroy instance, create new VectorStore on same dir, init, re-index — verify all skipped                                        |
| p3-u04-vector-store.AC3.1 | search returns SemanticResult with uid, score, recipeName      | unit        | src/features/vector-store.test.ts             | Mock `embed()` to return `[1, 0, 0]`, mock `queryItems()` to return result items — verify `SemanticResult[]` shape with uid, score, and recipeName               |
| p3-u04-vector-store.AC3.1 | search returns SemanticResult with uid, score, recipeName      | integration | src/features/vector-store.test.integration.ts | Index recipes with deterministic embedder, search with query — verify results have correct uid, score, and recipeName fields                                     |
| p3-u04-vector-store.AC3.2 | search results ordered by descending cosine similarity         | unit        | src/features/vector-store.test.ts             | Mock `queryItems()` returning items in descending score order — verify output order matches                                                                      |
| p3-u04-vector-store.AC3.2 | search results ordered by descending cosine similarity         | integration | src/features/vector-store.test.integration.ts | Index recipes with different content, search — verify first result has highest score                                                                             |
| p3-u04-vector-store.AC3.3 | search on empty index returns []                               | unit        | src/features/vector-store.test.ts             | Mock `queryItems()` returns `[]` — verify `search()` returns `[]`                                                                                                |
| p3-u04-vector-store.AC4.1 | removeRecipe deletes from Vectra and hash map                  | unit        | src/features/vector-store.test.ts             | After indexing, call `removeRecipe(uid)` — verify `deleteItem(uid)` called and hash map no longer contains uid                                                   |
| p3-u04-vector-store.AC4.1 | removeRecipe deletes from Vectra and hash map                  | integration | src/features/vector-store.test.integration.ts | Index 2 recipes, remove one, search — verify only remaining recipe found                                                                                         |
| p3-u04-vector-store.AC4.2 | removeRecipe persists updated hash map                         | unit        | src/features/vector-store.test.ts             | After removal, read `hash-index.json` from temp dir — verify uid absent from file                                                                                |
| p3-u04-vector-store.AC4.3 | removeRecipe for non-existent uid does not throw               | unit        | src/features/vector-store.test.ts             | Call `removeRecipe("nonexistent")` — verify no exception thrown (Vectra `deleteItem` silently no-ops)                                                            |
| p3-u04-vector-store.AC5.1 | contentHash produces stable SHA-256 hex digest                 | unit        | src/features/vector-store.test.ts             | Call `contentHash("hello")` twice — assert same 64-char hex result                                                                                               |
| p3-u04-vector-store.AC5.1 | contentHash determinism (property-based)                       | property    | src/features/vector-store.property.test.ts    | For any string `s`, `contentHash(s) === contentHash(s)` — fast-check property                                                                                    |
| p3-u04-vector-store.AC5.1 | contentHash format (property-based)                            | property    | src/features/vector-store.property.test.ts    | Output is always a 64-character hex string matching `/^[0-9a-f]{64}$/` — fast-check property                                                                     |
| p3-u04-vector-store.AC5.1 | contentHash sensitivity (property-based)                       | property    | src/features/vector-store.property.test.ts    | For any two distinct non-empty strings `a !== b`, `contentHash(a) !== contentHash(b)` — fast-check property with `fc.pre(a !== b)`                               |
| p3-u04-vector-store.AC5.2 | directions change does not change content hash                 | unit        | src/features/vector-store.test.ts             | `makeRecipe({ directions: "A" })` vs `makeRecipe({ directions: "B" })` — both produce same hash via `contentHash(recipeToEmbeddingText(recipe, []))`             |
| p3-u04-vector-store.AC5.3 | ingredients change does change content hash                    | unit        | src/features/vector-store.test.ts             | `makeRecipe({ ingredients: "flour" })` vs `makeRecipe({ ingredients: "sugar" })` — produce different hashes via `contentHash(recipeToEmbeddingText(recipe, []))` |

## Human Verification

| AC     | Criterion                                              | Why Not Automated | Verification Approach |
| ------ | ------------------------------------------------------ | ----------------- | --------------------- |
| (none) | All acceptance criteria are covered by automated tests | N/A               | N/A                   |
